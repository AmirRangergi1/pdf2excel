# Example environment variables for pdf2excel
# Set `LIARA_API_ENDPOINT` and `LIARA_API_KEY` to enable remote Liara/Gemini conversion.

# Liara / Gemini (remote AI service)
LIARA_API_ENDPOINT=
LIARA_API_KEY=
LIARA_MODEL=google/gemini-2.0-flash-lite-001

# Flask app
FLASK_SECRET=change-me
PORT=5000

# Optional local folders (the app uses defaults if not set)
UPLOAD_FOLDER=uploads
OUTPUT_FOLDER=outputs

# Debug flag (optional)
DEBUG=1

# When REMOTE_ONLY=1, the app WILL NOT fall back to local conversion.
# Set REMOTE_ONLY=0 to allow local fallback (not recommended if you want AI-only).
REMOTE_ONLY=1
